#include <stdio.h>
// Welcome to GTA 7 in Python tutorial! (Ignore the C syntax that sometimes shows up)

// TIP: Do not get confused by this file being a C extension, C extenstions support both Python and C code!

// Map = Big
// Graphics = PeakCinema
// If User == Hack: Ban User
// Ban = bombombombobmbom
// If this doesn't work, make sure you already toasted your microwave.


























#if _WIN32 || _WIN64
    #include <windows.h>
    int main(void) {printf("Loading Assets...\n"); Sleep(10000); printf("Graphics loaded. You might not see the game at first, it can take up to 10 years.\n") ;return 0;}
#else
    #include <unistd.h>
    int main(void) {printf("Loading Assets...\n"); sleep(10); printf("Graphics loaded. You might not see the game at first, it can take up to 10 years.\n"); return 0;}
#endif
